# ロギングのリファクタリング

ロギング処理が散在しているため、リファクタリングを行う。

## タスク
- `console.`を検索して、現状を理解する。
- `src/lib/logger.ts`を修正して、ロギング処理を集中させる。
- `debug`, `info`, `warn`, `error`の4段階のログレベルをサポートする。
- `debug`ログを全体的に強化する。
- `warn`, `error`ログを適切に追加して、詳細を記録するようにする。